```yaml
# SPDX-License-Identifier: GPL-3.0-or-later
# Copyright (C) 2026 OntoCoder Collective

version: "1.0.0"
description: "Systems must maintain balance between change and stability."
semantic_formulation: |
  Excessive rigidity (no change) or chaos (no stability) invalidates structural integrity.
  Valid output must demonstrate tension between persistence and adaptation.
hard_constraint: true
causal_dependencies:
  - energy_minimization
  - interaction
validation_rules:
  min_ontological_density: 0.5
  requires_social_context: true
  permits_override_in_crisis: false
```