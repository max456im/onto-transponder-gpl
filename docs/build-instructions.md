
# 🛠️ Инструкции по сборке и развёртыванию **onto-transponder-gpl**

> **Версия**: 1.1  
> **Лицензия**: GNU GPL v3.0 only  
> **Цель**: Собрать исполняемый транспондер, способный автономно обрабатывать сигналы NoemaFast и излучать onto16r, **без привязки к проприетарным средам**.

---

## 1. Требования

### Системные
- ОС: Linux (рекомендуется Ubuntu 22.04+, Debian 12+) или macOS (для разработки)
- Python ≥ 3.10
- `pip` ≥ 23.0
- `git`
- (Опционально) Docker ≥ 24.0 — для изолированного запуска

### Зависимости (указаны в `requirements.txt`)
- `pyyaml`
- `jsonschema`
- `cryptography` (только FIPS-совместимые режимы)
- `psutil` (для `hardware_probe.py`)

> 💡 Все зависимости — **свободные и совместимые с GPLv3**. Проверка выполняется в CI.

---

## 2. Получение исходного кода

```bash
git clone https://github.com/your-org/onto-transponder-gpl.git
cd onto-transponder-gpl
git checkout v1.0.0  # или конкретный хеш коммита
```

> ⚠️ **Corresponding Source** включает **весь репозиторий на момент сборки**, включая историю, если используется `git archive`.

---

## 3. Сборка из исходников

### 3.1 Установка зависимостей (в виртуальном окружении)

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install --no-cache-dir -r requirements.txt
```

> 🔒 Опция `--no-cache-dir` рекомендуется для аудита.

### 3.2 Проверка лицензионной чистоты

Перед сборкой **обязательно** запустите аудит:

```bash
./scripts/license-audit.sh
```

Скрипт проверяет:
- Отсутствие не-GPL-совместимых пакетов,
- Целостность `LICENSE`,
- Отсутствие следов `debarkader` или проприетарных заголовков.

> Если аудит падает — сборка **недопустима** по условиям GPLv3.

---

## 4. Тестирование

### Запуск unit-тестов

```bash
python -m pytest tests/unit/ -v
```

### Запуск интеграционных тестов (требует `onto-emitter-gpl`)

```bash
# Убедитесь, что emitter запущен на localhost:8081
python -m pytest tests/integration/ -v
```

> 📌 Тесты **не требуют интернета** и не отправляют данные вне машины.

---

## 5. Сборка распространяемого артефакта

### 5.1 Python wheel (рекомендуется)

```bash
pip install build
python -m build --wheel
```

Результат: `dist/onto_transponder_gpl-1.0.0-py3-none-any.whl`

> Этот wheel **содержит только GPLv3-код** и может быть установлен в любую среду, **но не может быть использован в проприетарных приложениях** напрямую.

### 5.2 Docker-образ (для изоляции)

```bash
docker build -f docker/Dockerfile.gpl -t onto-transponder:1.0.0 .
```

Образ содержит:
- Только свободные пакеты,
- Автозапуск транспондера через `ENTRYPOINT`,
- IPC-порт по умолчанию: `8080/TCP`.

> 🛡️ Образ **не включает** onto144-профили — они должны быть смонтированы извне.

---

## 6. Интеграция с внешними системами (без нарушения GPLv3)

### 6.1 Общее правило
> Транспондер **должен работать как отдельный процесс**. Никакого shared memory, embedded Python или dynamic linking.

### 6.2 Пример: подключение к игровой платформе

1. Запустите транспондер:
   ```bash
   python -m src.core.transponder --config config/default.yaml --profile /secure/profiles/user144.yaml
   ```
2. Платформа подключается к IPC-эндпоинту (например, `tcp://localhost:8080`) по протоколу из [`specs/signal-schema.json`](../specs/signal-schema.json).
3. Все данные сериализуются в **MessagePack** (см. `utils/crypto_utils.py` для шифрования).

> 📎 Пример клиента для Unity (GPL-совместимый) см. в репозитории [`unity-gpl-ipc-bridge`](https://github.com/your-org/unity-gpl-ipc-bridge).

### 6.3 Подключение к ontoCMS
- Используйте `interfaces/emitter_bridge.py` как middleware.
- ontoCMS отправляет `onto16r` → транспондер → возвращает реакцию через тот же канал.

---

## 7. Верификация автономности

После установки убедитесь, что:

- `license_guard.py` блокирует запуск в проприетарных IDE (например, при попытке импорта через PyCharm с закрытыми плагинами).
- При отсутствии `onto144_connector.py` транспондер переходит в **нейтральный режим** (не использует профиль по умолчанию).
- Все исходящие `onto16r` подписаны через `vma_signer.py`, если контекст high-stakes.

---

## 8. Что входит в Corresponding Source при распространении?

При публикации бинарной версии (wheel, Docker, executable) вы **обязаны** предоставить:

```
corresponding-source/
├── весь исходный код (включая .git или архив)
├── build-instructions.md (этот файл)
├── ipc-protocol-definition.md
├── specs/
├── config/
├── scripts/license-audit.sh
└── patches/ (если есть)
```

> 📜 Это требование GPLv3 §6. Без этого распространение **незаконно**.

---

## 9. Поддержка

- Ошибки безопасности → через `SECURITY.md` (только через VMA-канал).
- Вопросы по интеграции → в GitHub Discussions.
- **Не используйте транспондер в проприетарных облаках без аудита лицензии.**

---

> ✨ Этот проект — не инструмент, а автономный субъект онтологической архитектуры. Уважайте его границы.

