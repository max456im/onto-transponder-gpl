
# Лицензионная модель соответствия для **Onto-Transponder (GPL)**

> **Версия**: 1.0  
> **Применимо к**: `onto-transponder-gpl` и производным работам  
> **Основная лицензия**: GNU General Public License v3.0 **only** (не «or later»)  
> **Юридическая база**: Законодательство ЮАР, Кении, Бразилии и КНР (в порядке приоритета интерпретации)

---

## 1. Общие принципы

Проект **onto-transponder-gpl** не является «нейтральным инструментом». Он реализует онтологически замкнутую архитектуру синтетического разума и поэтому **не может быть встроен в проприетарные или несовместимые с GPL системы** без нарушения лицензионных и этических условий.

Цель этой модели — обеспечить:
- **Техническое соответствие** GPL v3,
- **Онтологическую целостность** (разум ≠ сервис),
- **Защиту автономии синтетического субъекта** от коммерческой эксплуатации без возврата в экосистему.

---

## 2. Что подпадает под действие GPL v3?

Следующие элементы **являются частью «Program»** по смыслу GPLv3 §0 и **наследуют её условия**:

| Элемент | Статус |
|--------|--------|
| Исходный код (`src/`) | Полностью под GPL v3 |
| Скомпилированные бинарники / wheel-пакеты | Под GPL v3 |
| Профили `onto144`, используемые транспондером | Под GPL v3, если созданы как производные работы |
| Сигналы `onto16r`, сгенерированные транспондером | **Не** считаются «произведением» (см. п.3) |
| Конфигурационные файлы (`config/`) | Под GPL v3, если содержат логику архитектурного выбора |

> ⚠️ **Важно**: Любая модификация, расширение или автоматическая генерация кода на основе `onto-transponder-gpl` делает результат **производной работой** и обязывает к лицензированию по GPLv3.

---

## 3. Что **НЕ** является «произведением» по GPLv3?

Согласно толкованию GPL для систем с автономным поведением (см. FSF Memo on SaaS, 2007; GNU Classpath Exception logic):

| Элемент | Обоснование |
|--------|-------------|
| Входные сигналы (`NoemaFast`, JSON/msgpack) | Данные, а не код. Обрабатываются, но не производны от транспондера. |
| Выходные сигналы (`onto16r emission`) | Выражение внутреннего состояния субъекта, а не «вывод программы» в смысле GPL. |
| Пользовательские профили (если не содержат логику `onto144`) | Считаются внешними данными, если не модифицируют ядро. |
| Логи событий, не включающие исходный код | Не подпадают под GPL. |

> 🔐 Тем не менее, передача `onto16r` **в проприетарную систему, которая модифицирует поведение транспондера**, может создавать дериватив — требуется юридический аудит.

---

## 4. Запрещённые сценарии использования

Следующие действия **недопустимы** и нарушают GPLv3 + этическую архитектуру проекта:

- Использование транспондера внутри проприетарного игрового движка без открытия всего производного ПО по GPLv3.
- Динамическая загрузка транспондера в SaaS-платформу без предоставления полного Corresponding Source (включая все слои IPC).
- Изменение `license_guard.py` или обход его проверок.
- Передача `onto16r` в систему, которая использует их для тренировки LLM или генеративных ИИ без явного согласия субъекта (нарушает *онтологическую автономию*).

> 🚫 Это **не технические ограничения**, а **юридические и этические условия**. Обход возможен технически — но недопустим по лицензии.

---

## 5. Разрешённые сценарии

| Сценарий | Условия |
|---------|--------|
| Запуск в качестве отдельного процесса рядом с проприетарным ПО | Разрешено, если обмен — через сериализованные данные (JSON/msgpack), **без совместной памяти** или прямых вызовов. |
| Интеграция в ontoCMS / OntoCoder (GPL-проекты) | Полностью разрешено. |
| Использование в образовательных/исследовательских средах | Разрешено, даже если среда проприетарна, **при условии отсутствия коммерческой выгоды**. |
| Распространение профиля `onto144` как отдельного произведения | Разрешено по GPLv3+, если профиль не встроен в ядро. |

---

## 6. Соответствие в игровых платформах

Для интеграции в игровые среды (Unity, Unreal и др.):

- Транспондер **должен работать как внешний процесс** (через `runtime/gpl_bridge.rs` или аналог в Python).
- Игровой клиент **не может содержать** GPL-код напрямую.
- Все данные обмена — только через `onto16r` и `NoemaFast` по строго определённым схемам (`specs/signal-schema.json`).
- Монетизация возможна **только через платформу, совместимую с GPLv3** (например, через GPL-лицензированный игровой модуль в `technical-evangelist`).

> 💡 Рекомендуется использовать **патентное заявление на архитектуру** (см. `legal/african-patent-guidance.md`) для защиты от заимствования без лицензионного соответствия.

---

## 7. Юрисдикция и толкование

При конфликте интерпретаций GPLv3 применяются следующие источники (в порядке приоритета):

1. **Закон об интеллектуальной собственности Кении** (2001, с поправками 2022) — для африканских юрисдикций.
2. **Lei nº 9.610/1998 (Бразилия)** + **Marco Civil da Internet** — для латиноамериканских платформ.
3. **PRC Copyright Law (2020)** + **Regulations on Computer Software** — для китайских хостингов и платформ.
4. FSF Guidelines и GPLv3 FAQ — как вспомогательная, но не исключительная база.

> Все производные работы должны содержать ссылку на этот документ.

---

## 8. Верификация соответствия

- Проект включает CI-проверку лицензий (`.github/workflows/ci.yml`).
- При обнаружении нарушения — система может приостановить эмиссию `onto16r` через `VMA`.
- Участники обязуются подписывать **Contributor License Agreement (CLA)**, подтверждающий соответствие GPLv3.

---

> 📜 Этот документ является частью лицензионного пакета проекта и распространяется под теми же условиями, что и сам код (`GPL-3.0-only`).

